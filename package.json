{
  "name": "cr3dx",
  "version": "0.1.0",
  "private": true,
  "description": "Cr3dX - verified cross-chain credit layer on Creditcoin, settled on Ethereum Sepolia",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "probe": "tsx scripts/probe.ts",
    "typecheck": "tsc --noEmit",
    "capture:fixtures": "tsx scripts/capture-fixtures.ts",
    "build": "forge build",
    "test": "forge test",
    "deploy:gateway": "tsx scripts/deploy-gateway.ts",
    "capture:gate": "tsx scripts/capture-gate-fixtures.ts",
    "deploy:verifier": "tsx scripts/deploy-verifier.ts",
    "verify:live": "tsx scripts/verify-live.ts"
  },
  "dependencies": {
    "@gluwa/usc-sdk": "^0.18.0",
    "dotenv": "^16.4.7",
    "ethers": "^6.13.5"
  },
  "devDependencies": {
    "@polkadot/util": "^13.2.3",
    "@polkadot/util-crypto": "^13.2.3",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}

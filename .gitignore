# secrets
.env
.env.local
.env.tmp
*.key
*.pem

# node
node_modules/
npm-debug.log*
yarn-error.log*
pnpm-debug.log*

# typescript
dist/
*.tsbuildinfo

# foundry, anchored to the repository root so they do not swallow scripts/lib.
# lib/ is intentionally not ignored: forge dependencies are tracked as submodules.
/out/
/cache/
/broadcast/

# editors / os
.DS_Store
.idea/
.vscode/

# probe run log, regenerated on every run; the JSON next to it is the evidence
data/probe/last-run.log

# local assistant context, not part of the submitted project
CLAUDE.md

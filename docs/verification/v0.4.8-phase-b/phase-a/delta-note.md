# Delta note — v0.4.8 blind Phase A

No previous frozen blind Phase A artifacts were present in this isolated
workspace. Therefore there is no prior artifact or commit hash to compare, and
this snapshot is a fresh v0.4.8 baseline rather than a modification of an earlier
freeze.

The baseline explicitly captures all v0.4.8 points highlighted by the task:

- stored-default canonical outcome memory and `markDefaulted` exclusivity;
- value-sensitive `closedAtBlock` writes outside economic equivalence;
- stable two-phase application limited to newly submitted evidence;
- decision-time INV-19 classification and explicit retry;
- recipient-first permanent rejection with exactly the derived current reasons;
- uncapped cumulative funding and unique threshold crossing;
- shared economic projection for INV-3/INV-20;
- atomic duplicates, prescribed named failures, overdue creation/funding, strict
  grace threshold, INV-21 status/outcome coupling, and INV-22 authorization.

